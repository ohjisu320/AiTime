services:
  ai-task3:
    build:
      context: ../AI/services/face-to-face-name-response
      dockerfile: ../../../docker/dockerfiles/Dockerfile.ai_task3
    container_name: ai-task3
    ports:
      - "8003:8003"
    volumes:
      - ../AI/services/face-to-face-name-response/app:/app/app
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - cmn-network
    restart: unless-stopped
    command: ["python", "-m", "app.worker"]
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f 'python -m app.worker' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  cmn-network:
    external: true

