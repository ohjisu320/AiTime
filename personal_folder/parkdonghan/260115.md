## 2026.01.15 한 일 정리

### 1) 자문 섭외 진행 (이메일/오프라인)

* 팀원 **각자 약 15건씩 이메일 발송**, 나는 **1건 답장 수신**

  * 다만 **성인/노인 전문**이라 *소아 전문의에게 컨택 권유*를 받음
* 점심 중 **근처 정신건강의원 방문 논의 → 즉시 실행**

  * **사전 공유/보고가 미흡**했고, 이 부분에 대해 **피드백 수령**
* 팀 6명을 **2개 팀으로 분리**, 내가 속한 팀은 **병원 3곳 방문**

  * 결과: **소아 진료 X**, **학생 신분으로 자문 거절** 등으로 성과 제한적

### 2) 기술 학습/실행 (Jenkins)

* 이메일 회신 대기 시간 동안 **기술 학습 분담**
* 나는 **어제 공부한 Jenkins를 실제로 실행시키는 과정**을 진행하며 흐름을 점검

### 3) 방향성 회의 (자문 필요성 vs 문헌 근거)

* 오후까지 도착한 이메일들도 대부분 **“소아 전문의에게 연락 권유”** 내용
* 그래서 팀 내에서

  * “자문이 꼭 필요한가?”
  * “논문/의학 서적 근거로 대체 가능한가?”
    를 두고 회의 진행
* 나는 **‘우리가 문제를 해결했다고 말할 수 있는 출발점’이 자문**이라고 의견 제시

### 4) 새로운 접점 확보 (전문의 연결)

* 정현이 **자문 서비스에서 소아청소년정신과 전문의 발견 → 팀 단체 문의**
* 추가 루트도 동시 진행

  * 윤영 누나는 지인 통해 **소아청소년정신과 전문의에게 메일 발송 완료**
  * 상인 코치님도 지인에게 **이메일 전달**
    * 해당 전문의가 **2026.01.16(금) 14시 당직** → 이후 확인 시 **팀에 연락** 주실 예정

### 5) 역할 변화 (기술 리더 선정)

* 라이브러리 방송에서 기술 리더 선정을 권장한 내용을 계기로
  → 내가 **기술 리더로 선정**
* 동료의 믿음에 부끄럽지 않게 앞으로 기술 리더 역할에 맞게 혹여 개입할 일이 있을 때 **팀 동의 기반의 결정**, **조율/기여**에 힘 쏟으며 공통 프로젝트를 마무리 하고 싶다.

### 6) AI 논의 및 이번 주 목표 설정

* 18시 이후 정현과 **AI 개발 방향** 논의
* 팀 목표: **2/2까지 1차 마무리**, 실 개발 기간 **약 2주 반**으로 인식 정리
* 모델 개발 흐름 합의

  * **Task 정의 → 후보 모델 선정 → I/O 형식 정의 → 학습(로깅 포함) → 지표(metics) 평가/해석 → 다음 학습 방향 결정**
  * 서비스 관점: **학습 이후 API 요청 기반 추론 구현**도 필요
* 이번 주 목표: **각자 맡은 AI task를 명확히 정의**
* 논의 외 추가로 전체 프로젝트 흐름(엔드투엔드)도 Jenkins 공부처럼 이번 주에 함께 정리하는 것이 좋을 것 같아 이 부분도 다뤄볼 예정

---
어제 공부한 내용을 따라서 간단한 fastAPI 프로젝트를 바탕으로 jenkins를 이용해 CI/CD의 경험을 해보았다.

# 0) 진행한 전체 과정

* Jenkins가 파이프라인을 돌려서

  * ruff + pytest 통과
  * Docker 이미지 빌드
  * Local Registry(`localhost:5000`)에 push
  * 로컬에 컨테이너 배포(`localhost:8000`)
  * `/health` 200 확인

---

# 1) 환경

* Docker Desktop (Linux 컨테이너)
* WSL2
* PowerShell
* 프로젝트 폴더 구조

```
jenkins_fastapi_ci_cd_example/
  agents/
  fastapi-app/
  jenkins-controller/
```

---

# 2) 로컬 Docker Registry 띄우기

```powershell
docker run -d --name local-registry --restart unless-stopped -p 5000:5000 registry:2
```

확인:

```powershell
curl http://localhost:5000/v2/_catalog
```

## 1) `docker run`

* **컨테이너를 새로 만들어서 실행**하는 명령

## 2) `-d` (detached)

* **백그라운드로 실행**하라는 뜻.

## 3) `--name local-registry`

* 컨테이너 이름을 `local-registry`로 지정.

## 4) `--restart unless-stopped`

* Docker 데몬이 재시작되면 컨테이너도 자동으로 다시 켜지게 함.
* 단, 내가 명시적으로 `docker stop local-registry`로 꺼두면 그때는 자동 재시작 안 함.

## 5) `-p 5000:5000`

* 포트 매핑(포트 포워딩)
* 형식은 **`호스트포트:컨테이너포트`**
* 즉:

  * 내 PC의 `localhost:5000`으로 들어오면
  * 컨테이너 내부의 `5000` 포트로 전달됨
* 그래서 레지스트리에 `localhost:5000/v2/...`로 접근 가능

## 6) `registry:2`

* 실행할 **이미지 이름**.
* `registry`라는 이미지의 **태그(tag) `2`** 버전.
* Docker Hub의 “Docker Registry(이미지 저장 서버)” 공식 이미지로, 이게 실제 레지스트리 프로그램을 담고 있음.

---

# 3) Jenkins Controller 실행
`.env`를 만들었고 docker yml에 경로를 포함해 실행

`jenkins-controller` 폴더로 이동해서:

```powershell
cd .\jenkins-controller
docker compose up -d
```

Jenkins 접속:

* `http://localhost:8080`

드디어 첫 Jenkins 화면을 띄웠고

`.env`에 정의해둔 ID,PW로 로그인 완료
---

# 4) Jenkins에서 Agent 노드 2개 만들기

Jenkins UI:

1. **Manage Jenkins**
2. **Nodes** (또는 Nodes and Clouds)
3. **New Node**
4. 이름: `build-agent-1` / Type: **Permanent Agent**

   * Remote root directory: `/home/jenkins/agent`
   * Labels: `build && docker`
   * Launch method: **Launch agent by connecting it to the controller**
   * Save

똑같이 하나 더:

* 이름: `deploy-agent-1`
* Labels: `deploy`
* Save

각 노드 화면에서 **agent secret**를 확인! docker run 할 때 사용:

* Nodes → 해당 노드 클릭 → “Launch agent” 화면에서 `-secret XXXXX` 값

---

# 5) Agent 이미지 빌드

프로젝트 루트로 이동:

```powershell
cd ..  # (jenkins-controller 폴더에서 나왔다면)
```

build-agent:
```docker
FROM jenkins/inbound-agent:latest-jdk17

USER root

# 기본 유틸 + Python 도구
RUN apt-get update && apt-get install -y --no-install-recommends \
    ca-certificates curl gnupg git \
    python3 python3-pip python3-venv \
 && rm -rf /var/lib/apt/lists/*

# --- Docker 공식 repo 추가 + 최신 docker-ce-cli 설치 ---
RUN install -m 0755 -d /etc/apt/keyrings && \
    curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg && \
    chmod a+r /etc/apt/keyrings/docker.gpg && \
    . /etc/os-release && \
    echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
    https://download.docker.com/linux/debian ${VERSION_CODENAME} stable" \
    > /etc/apt/sources.list.d/docker.list && \
    apt-get update && \
    apt-get install -y --no-install-recommends docker-ce-cli docker-buildx-plugin docker-compose-plugin && \
    rm -rf /var/lib/apt/lists/*

USER jenkins

```

deploy-agent:
```docker
FROM jenkins/inbound-agent:latest-jdk17

USER root
RUN apt-get update && apt-get install -y --no-install-recommends \
    ca-certificates curl gnupg \
 && rm -rf /var/lib/apt/lists/*

RUN install -m 0755 -d /etc/apt/keyrings && \
    curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg && \
    chmod a+r /etc/apt/keyrings/docker.gpg && \
    . /etc/os-release && \
    echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
    https://download.docker.com/linux/debian ${VERSION_CODENAME} stable" \
    > /etc/apt/sources.list.d/docker.list && \
    apt-get update && \
    apt-get install -y --no-install-recommends docker-ce-cli docker-buildx-plugin docker-compose-plugin && \
    rm -rf /var/lib/apt/lists/*

USER jenkins

```


에이전트 이미지 빌드:

```powershell
docker build -t jenkins-build-agent -f agents/images/Dockerfile.build .
docker build -t jenkins-deploy-agent -f agents/images/Dockerfile.deploy .
```
---

# 6) Agent 컨테이너 실행 (PowerShell, root로)

프로젝트 루트(= `fastapi-app` 폴더가 보이는 위치)에서 실행.

### build-agent-1

```powershell
docker run -d --name build-agent-1 --restart unless-stopped `
  --user root `
  -e JENKINS_URL="http://host.docker.internal:8080/" `
  -e JENKINS_AGENT_NAME="build-agent-1" `
  -e JENKINS_SECRET="<BUILD_SECRET>" `
  -v /var/run/docker.sock:/var/run/docker.sock `
  -v "$((Get-Location).Path)\fastapi-app:/src/fastapi-app" `
  jenkins-build-agent
```

### deploy-agent-1

```powershell
docker run -d --name deploy-agent-1 --restart unless-stopped `
  --user root `
  -e JENKINS_URL="http://host.docker.internal:8080/" `
  -e JENKINS_AGENT_NAME="deploy-agent-1" `
  -e JENKINS_SECRET="<DEPLOY_SECRET>" `
  -v /var/run/docker.sock:/var/run/docker.sock `
  -v "$((Get-Location).Path)\fastapi-app:/src/fastapi-app" `
  jenkins-deploy-agent
```

✅ 확인:

```powershell
docker ps
```

```powershell
CONTAINER ID   IMAGE                                 COMMAND                   CREATED          STATUS             PORTS           
                                                                               NAMES
4b7c9348e807   localhost:5000/fastapi-app:local-14   "uvicorn app.main:ap…"   51 minutes ago   Up 51 minutes      0.0.0.0:8000->8000/tcp, [::]:8000->8000/tcp                                                    fastapi-app
96a906c31015   jenkins-deploy-agent                  "/usr/local/bin/jenk…"   55 minutes ago   Up 55 minutes                       
                                                                              deploy-agent-1
d4ad96de6da0   jenkins-build-agent                   "/usr/local/bin/jenk…"   56 minutes ago   Up 56 minutes                       
                                                                              build-agent-1
927409a0923b   registry:2                            "/entrypoint.sh /etc…"   2 hours ago      Up 2 hours         0.0.0.0:5000->5000/tcp, [::]:5000->5000/tcp                                                    local-registry
042a824aa8a3   jenkins-controller-jenkins            "/usr/bin/tini -- /u…"   2 hours ago      Up About an hour   0.0.0.0:8080->8080/tcp, [::]:8080->8080/tcp, 0.0.0.0:50000->50000/tcp, [::]:50000->50000/tcp   jenkins-controller

```
Jenkins UI에서 Nodes로 가서 두 노드가 **Online**이면 성공.
---

# 7) Jenkins Pipeline Job 만들기

Jenkins UI:

1. New Item
2. 이름: `fastapi-local-test`
3. **Pipeline** 선택
4. Pipeline script에 아래 붙여넣기
5. Save

---

# 8) Pipeline Script

```groovy
pipeline {
  agent none
  options { disableConcurrentBuilds() }

  environment {
    APP_NAME  = "fastapi-app"
    REGISTRY  = "localhost:5000"
    IMAGE_TAG = "local-${BUILD_NUMBER}"
    IMAGE_URI = "${REGISTRY}/${APP_NAME}:${IMAGE_TAG}"

    SOURCE_DIR = "/src/fastapi-app"  // 여기엔 쓰지 않음(읽기만)
    HOST_BASE  = "http://host.docker.internal:8000"
  }

  stages {
    stage("CI: Lint & Test") {
      agent { label "build && docker" }
      steps {
        deleteDir()
        sh '''
          set -e
          cp -a ${SOURCE_DIR}/. .
        
          python3 -m venv .venv
          . .venv/bin/activate
        
          pip install -U pip
          pip install -r requirements.txt -r requirements-dev.txt
        
          ruff check .
          PYTHONPATH=. pytest -q
        '''

      }
    }

    stage("Build & Push (Local Registry)") {
      agent { label "build && docker" }
      steps {
        deleteDir()
        sh '''
          set -e
          cp -a ${SOURCE_DIR}/. .
          docker build -t ${IMAGE_URI} .
          docker push ${IMAGE_URI}
        '''
      }
    }

    stage("Deploy (Local Docker)") {
      agent { label "deploy" }
      steps {
        sh '''
          set -e
          docker pull ${IMAGE_URI}
          docker rm -f fastapi-app 2>/dev/null || true
          docker run -d --name fastapi-app --restart unless-stopped \
            -p 8000:8000 ${IMAGE_URI}
        '''
      }
    }


    stage("Verify (health/smoke)") {
      agent { label "deploy" }
      steps {
        sh '''
          set -e
          curl -fsS ${HOST_BASE}/health
          curl -fsS -X POST ${HOST_BASE}/predict \
            -H "Content-Type: application/json" \
            -d '{"text":"good"}' > /dev/null
        '''
      }
    }
  }
}

```

---

# 9) 실행

Jenkins에서 **Build Now** 클릭.

성공하면 마지막 Verify 단계까지 통과.

14번만에 통과하였다.
---

# 10) 로컬에서 최종 확인

health check:
```powershell
Invoke-RestMethod http://localhost:8000/health
```

정상 출력:

```json
{"status":"ok"}
```

predict:
```powershell
Invoke-RestMethod -Method Post `
  -Uri "http://localhost:8000/predict" `
  -ContentType "application/json" `
  -Body '{"text":"good"}'
```
정상 출력:
```
label    score
-----    -----
positive   0.7
```
---

내일 아직 공부 못한 코드들 정리하기